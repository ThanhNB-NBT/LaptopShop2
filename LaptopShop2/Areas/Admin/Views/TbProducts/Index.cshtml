@model IPagedList<LaptopShop2.Models.TbProduct>
@using X.PagedList;
@using X.PagedList.Mvc.Core;

@{
    ViewData["Title"] = "Quản lý sản phẩm";
    var successMessage = TempData["SuccessMessage"] as string;
    var errorMessage = TempData["ErrorMessage"] as string;
}
<style>
    .truncate-text {
        overflow: hidden;
        white-space: nowrap;
        text-overflow: ellipsis;
        max-width: 85px;
    }
</style>

<div class="container-fluid">
    <div class="row">
        <div class="d-flex align-items-stretch">
            <div class="card w-100">
                <div class="card-body p-4">
                    <h5 class="card-title fw-semibold mb-4">Quản lý Sản phẩm</h5>
                    @if (!string.IsNullOrEmpty(successMessage))
                    {
                        <div id="successAlert" class="alert alert-success">
                            @successMessage
                        </div>
                    }
                    @if (!string.IsNullOrEmpty(errorMessage))
                    {
                        <div id="errorAlert" class="alert alert-danger">
                            @errorMessage
                        </div>
                    }
                    <div class="row mb-3">
                        <div class="col-sm-6">
                            <p>
                                <a asp-action="Create" class="btn btn-primary mb-3">Thêm mới</a>
                            </p>
                        </div>
                        <div class="col-sm-6">
                            <form method="get" action="@Url.Action("Index")" class="form-inline float-end">
                                <div class="input-group">
                                    <input type="text" class="form-control" name="searchString" placeholder="Nhập từ khóa...">
                                    <button type="submit" class="btn btn-primary">Tìm kiếm</button>
                                </div>
                            </form>
                        </div>
                    </div>
                    <div class="table-responsive">
                        <table class="table text-nowrap mb-0 align-middle">
                            <thead class="text-dark fs-4">
                                <tr>
                                    <th class="border-bottom-0">
                                        <h6 class="fw-semibold mb-0">STT</h6>
                                    </th>

                                    <th class="border-bottom-0">
                                        <h6 class="fw-semibold mb-0">Tên sản phẩm</h6>
                                    </th>

                                    <th class="border-bottom-0">
                                        <h6 class="fw-semibold mb-0">Hình ảnh</h6>
                                    </th>

                                    <th class="border-bottom-0">
                                        <h6 class="fw-semibold mb-0">Giá bán</h6>
                                    </th>
                                    @* <th class="border-bottom-0">
                                    <h6 class="fw-semibold mb-0">Giá khuyễn mãi</h6>
                                    </th> *@

                                    <th class="border-bottom-0">
                                        <h6 class="fw-semibold mb-0">Ngày tạo</h6>
                                    </th>

                                    <th class="border-bottom-0">
                                        <h6 class="fw-semibold mb-0">Số lượng</h6>
                                    </th>

                                    <th class="border-bottom-0">
                                        <h6 class="fw-semibold mb-0">Danh mục</h6>
                                    </th>

                                    <th class="border-bottom-0">
                                        <h6 class="fw-semibold mb-0">Hãng</h6>
                                    </th>

                                    @* <th class="border-bottom-0">
                                    <h6 class="fw-semibold mb-0">Sản phẩm mới</h6>
                                    </th>
                                    <th class="border-bottom-0">
                                    <h6 class="fw-semibold mb-0">Bán chạy</h6>
                                    </th> *@
                                    <th class="border-bottom-0">
                                        <h6 class="fw-semibold mb-0">Hiển thị</h6>
                                    </th>

                                    <th class="border-bottom-0">
                                        <h6 class="fw-semibold mb-0">Chức năng</h6>
                                    </th>
                                </tr>
                            </thead>
                            <tbody>
                                @{
                                    int i = 1;
                                }
                                @foreach (var item in Model)
                                {
                                    <tr data-id="@item.ProductId">
                                        <td class="border-bottom-0">
                                            <h6 class="fw-semibold mb-0">@(i++)</h6>
                                        </td>
                                        <td class="border-bottom-0">
                                            <h6 class="fw-semibold mb-0 truncate-text">@item.ProductName</h6>
                                        </td>
                                        <td class="border-bottom-0">
                                            <img style="max-width: 50px;height: 40px; object-fit: cover;" src="@item.Image" />
                                        </td>

                                        <td class="border-bottom-0">
                                            <h6 class="fw-semibold mb-0 truncate-text">@String.Format("{0:N0}", @item.Price)</h6>
                                        </td>

                                        <td class="border-bottom-0">
                                            <h6 class="fw-semibold mb-0">@item.CreatedDate?.ToString("dd-MM-yyyy")</h6>
                                        </td>
                                        <td class="border-bottom-0">
                                            <h6 class="fw-semibold mb-0 truncate-text text-center align-middle">@item.Quantity</h6>
                                        </td>
                                        <td class="border-bottom-0">
                                            @if (item.CategoryProductId != null)
                                            {
                                                <h6 class="fw-semibold mb-0 truncate-text">@item.CategoryProduct.Name</h6>
                                            }
                                            else
                                            {
                                                <h6 class="fw-semibold mb-0">Không</h6>
                                            }
                                        </td>
                                        <td class="border-bottom-0">
                                            @if (item.Brand != null)
                                            {
                                                <h6 class="fw-semibold mb-0 truncate-text">@item.Brand.Name</h6>
                                            }
                                            else
                                            {
                                                <h6 class="fw-semibold mb-0">Không</h6>
                                            }
                                        </td>

                                        <td class="border-bottom-0">
                                            <div class="d-flex align-items-center gap-2">
                                                @if (item.IsActive)
                                                {
                                                    <span class="badge bg-secondary rounded-3 fw-semibold">Có</span>
                                                }
                                                else
                                                {
                                                    <span class="badge bg-danger rounded-3 fw-semibold">Không</span>
                                                }
                                            </div>
                                        </td>

                                        <td class="border-bottom-0">
                                            <a asp-action="Edit" asp-route-id="@item.ProductId" class="btn btn-danger btn-sm btn-edit">Sửa</a> |
                                            <a asp-action="Details" asp-route-id="@item.ProductId" class="btn btn-info btn-sm btn-detail">Chi tiết</a> |
                                            <a href="#" class="btn btn-warning btn-sm btn-delete" data-id="@item.ProductId">Xóa</a>
                                            <!-- Form ẩn để thực hiện hành động xóa -->
                                            <form id="deleteForm-@item.ProductId" method="post" action="@Url.Action("Delete", "TbProducts", new { id = item.ProductId })" style="display: none;">
                                                @Html.AntiForgeryToken()
                                            </form>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    <div class="pagination">
                        @Html.PagedListPager(Model, page => Url.Action("Index", new { page }), new PagedListRenderOptions { Display = PagedListDisplayMode.IfNeeded, LiElementClasses = new[] { "page-link" } })
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <script>
        $(document).ready(function () {
            $(".btn-delete").click(function (e) {
                e.preventDefault(); // Ngăn chặn hành động mặc định của thẻ <a>
                var productId = $(this).data("id");
                if (confirm("Bạn có chắc chắn muốn xóa?")) {
                    $("#deleteForm-" + productId).submit(); // Nộp form ẩn tương ứng
                }
            });
        });
    </script>
}